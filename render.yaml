services:
  - type: web
    name: checksim-relay
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: python relay.py
    plan: free
    envVars:
      - key: PORT
        value: 10000
